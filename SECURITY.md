# 安全问题反馈

## 支持范围

只对 **`main` 分支最新提交**提供安全修复；历史提交、第三方分支与自行修改过的部署不在支持范围内。

## 怎么反馈

**请不要在公开 Issue 里披露安全问题细节。**

1. 首选：在仓库的 **Security → Report a vulnerability** 提交私有漏洞报告
   （若该入口不可用，说明仓库未开启私有报告，请走第 2 种方式）。
2. 备选：开一个**不含任何复现细节**的 Issue，说明"有一个安全问题想私下沟通"，
   维护者会回复你一个私下渠道。

请在报告中包含：影响版本 / 影响哪个接口或页面 / 复现步骤 / 你认为的严重程度。
**不要**在报告里附带真实账号、真实持仓或他人的数据。

## 本项目的安全模型（先说清楚边界）

- **没有服务端账号体系**：账号、密码、自选股、设置全部只存在**你自己浏览器的 localStorage**，
  服务端不保存、也不会同步到任何地方。因此"账号被盗"这类问题通常属于浏览器侧问题。
- **没有数据库**：服务端持久化只有两类文件 —— 全市场快照与预测台账（`QP_DATA_DIR`，默认 `.cache/`）。
- **仓库不含密钥**：`*.pem` / `*.key` / `cert/` 等已被 `.gitignore` 排除，也不接受提交。
  东方财富搜索接口的 token 是网页前端公开使用的固定值（非私钥），可用 `EM_SEARCH_TOKEN` 覆盖。
- **服务端会代理外部接口**：`/api/*` 的数据来自东方财富 / 腾讯公开接口，接口本身可用性不由本项目保证。
- **仅本机使用时**：默认监听所有网卡（`server.listen(PORT)`），本机自用没问题；
  但**公网部署请务必按下面的建议加固**。

## 已修复并已固化为回归测试的问题

透明起见列出，每一项都有对应断言（`_smoke/static-guard.js`），防止回归：

| 问题 | 影响 | 处置 |
|---|---|---|
| 静态托管越界读取 | 路径守卫用字符串前缀判断（`fp.startsWith(ROOT)`），兄弟路径会被误判为合法：`GET /..%2fquantpick-update.zip` 曾能下载到**应用目录之外**的文件 | 改为 `fp === ROOT \|\| fp.startsWith(ROOT + path.sep)`；断言见 `static-guard.js` |
| `.git` 元数据暴露 | `/.git/config`、`/.git/objects/*` 可被读取，攻击者可还原**完整提交历史**（含曾误提交后删除的文件） | 静态托管屏蔽 `.git/`；断言见 `static-guard.js` |
| 畸形路径返回 500 | `GET /%` 触发 `decodeURIComponent` 抛错，落到通用 500 | 明确返回 400 |

> 如果你在**旧版本**上部署过服务，请更新到 `main` 最新提交并重新部署后再对外提供服务。

## 部署加固建议

- **不要**把 `8090` 直接暴露到公网：用 Nginx 反代 + HTTPS，并只放通必要路径。
- 用**非 root 用户**运行（服务端不需要特权端口）。
- 建议给反代加上**限流**：应用会代理上游公开接口，被刷会连带触发上游限流。
- 应用目录**不要**与备份、证书、`.env` 等文件同级 —— 越界守卫已修复，但仍建议纵深防御。
- 桌面版（`QuantPick桌面版.exe`）解压到 `%LOCALAPPDATA%\QuantPick`，仅监听本机，
  如需清理直接删除该目录即可。
